{
  "name": "decisionguru",
  "version": "0.2.0",
  "private": true,
  "description": "Swiss tax-aware investment counterfactual analyzer — stocks vs. ETF decision engine",
  "workspaces": [
    "shared",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "cd backend && uv run uvicorn app.main:app --reload --host 127.0.0.1 --port 5178",
    "dev:frontend": "npm --workspace frontend run dev",
    "build": "npm --workspace frontend run build",
    "start": "cd backend && uv run gunicorn app.main:app -k uvicorn.workers.UvicornWorker -w 1 -b 0.0.0.0:5178"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
