import { EmptyState } from '../components/ui';

// Placeholder — replaced by the full advisory ("harmonize") module in a later commit.
export function Advisory() {
  return <EmptyState title="Advisory" hint="Rebalancing insights load here." />;
}
